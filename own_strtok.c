#include "holberton.h"
/**
 * own_strtok - extract words separated by delimitator
 * @str: string to splits
 * @delim: delimitator
 * Return: split the string
 */
char *own_strtok(char *str, const char *delim)
{
	return (NULL);
}
